model_name: asko_240611_station
features: [[0., 0., -0.20], # Center.
           [-0.21, -0.22, 0.], # Top left.
           [0.21, -0.22,  0.], # Top right.
           [0.21, 0.16, 0.], # Botttom right.
           [-0.21, 0.16, 0.]] # Bottom left.
placement_uncertainty: 0.01 # default uncertainty will be used if 0 (check feature_model/FeatureModel)
detection_tolerance: 0.1 # default uncertainty will be used if 0 (check feature_model/FeatureModel)
#placement_uncertainty: 0.001 # default uncertainty will be used if 0 (check feature_model/FeatureModel)
#detection_tolerance: 0 # default uncertainty will be used if 0 (check feature_model/FeatureModel)
